// Minimal stub for vite.config.ts verification
// Will be properly implemented in subtask-5-1
import { createRootRoute } from '@tanstack/react-router';

export const Route = createRootRoute({
  component: () => <div>Kaiten Plugin - Root Route Stub</div>,
});
